<!DOCTYPE html>
<html lang="en">
{{> head}}

<body class="hold-transition sidebar-mini layout-fixed">
    <div class="wrapper">
        {{> preloader}}
        {{> nav}}
        {{> aside}}
        <div class="content-wrapper">
            {{{body}}}
        </div>
    </div>


    {{> scripts}}
</body>

</html>